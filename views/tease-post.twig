{% extends "tease.twig" %}

{% block content %}
	<h2 class="h2 text-red-500"><a href3"{{post.link}}">{{ post.title }}</a></h2>
	{{ dump(post) }}
		<p>{{ post.excerpt({ words:5, read_more: "Keep reading" }) }}</p>
		{% if post.thumbnail.src %}
			<img src="{{ post.thumbnail.src }}" />
		{% endif %}
{% endblock %}